package event

// Standard topic constants used throughout the application.
const (
	TopicGitChange     = "git.change"
	TopicAdapterWatch  = "adapter.watch"
	TopicConfigChange  = "config.change"
	TopicProjectSwitch = "project.switch"
)
